
<%# <div id='<%#= dom_id listing %>
  <%# listing.photos.each do |photo| %>
    <%#= image_tag url_for(photo), width: 300, height:300 %>
  <%# end %>
  <%#= image_tag url_for(listing.photos[1]), width: 300, height:300 %>
  <%#= listing.title %>
  <%#= listing.location %>
  <%#= listing.description %>
  <%#= listing.price %>
  <%#= listing.num_guests %>
<%# </div> %>



      <li>
        <%= link_to listing_path(listing) do %>
          <%= image_tag url_for(listing.photos[1]), width: 300, height:300, class: "aspect-3/2 w-full rounded-2xl object-cover outline-1 -outline-offset-1 outline-black/5" %>
          <h3 class="mt-6 text-lg/8 font-semibold tracking-tight text-gray-900"><%= listing.title %></h3>
          <p class="text-base/7 text-gray-600"><%= listing.location %></p>
          <ul role="list" class="mt-6 flex gap-x-6">
            <li><%= listing.price %></li>
            <li><%= listing.num_guests %></li>
          </ul>
        <% end %>
      </li>
